import { HttpClient } from '@angular/common/http';
import { Injectable, inject } from '@angular/core';
import { Observable, of } from 'rxjs';
import { <%= className %>Data } from '../../models/<%= fileName %>.model';

@Injectable()
export class <%= className %>Service {
  httpClient = inject(HttpClient);

  /**
   * Stubbed until a real backend exists. To wire it up, import `environment`
   * from '@libs/environment' and return:
   *   this.httpClient.get<<%= className %>Data>(environment.baseUrl + '/<%= fileName %>')
   */
  fetch<%= className %>(): Observable<<%= className %>Data> {
    return of({ id: '1', label: '<%= className %> placeholder' });
  }
}
