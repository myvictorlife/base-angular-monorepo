// Entry point for the local Nx plugin. The generators are declared in
// `generators.json`; this file exists so the `@app/workspace-plugin` tsconfig
// path — which Nx uses to resolve local plugins — has a module to point at.
export {};
